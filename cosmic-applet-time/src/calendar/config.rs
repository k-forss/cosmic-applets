// SPDX-License-Identifier: GPL-3.0-only

//! Re-export calendar config types from the shared `cosmic-applets-config` crate
//! so that `cosmic-settings` can also use them.

pub use cosmic_applets_config::calendar::*;
